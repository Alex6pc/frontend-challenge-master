export type NavItem = {
  name: string;
  path: string;
  icon: string;
}

export type Color = {
  name: string;  
  color: string;
  price: number;
}

export type Motive = {
  name: string;
  img: string;
  price: number;
}